<Window x:Class="DEMANUFACTURE.KCC.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Error" 
        Height="280" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Resources/skull.ico"
        Background="{StaticResource BackgroundDarkBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Width="40" Height="40" CornerRadius="8" Background="#3A2626">
                <TextBlock Text="⚠" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource ErrorBrush}"/>
            </Border>
            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="ErrorTitle" Text="Error" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}"/>
                <TextBlock Text="An error occurred" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Error Message -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundLightBrush}" CornerRadius="8" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ErrorMessage" Text="" 
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           FontFamily="Consolas" FontSize="12"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="BtnCopy" Style="{StaticResource ModernButton}" Click="BtnCopy_Click" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock Text="Copy"/>
                </StackPanel>
            </Button>
            <Button x:Name="BtnClose" Style="{StaticResource PrimaryButton}" Click="BtnClose_Click">
                <TextBlock Text="Close"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
