<Window x:Class="McK.KCC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keeper Configuration Creator" 
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="Resources/mckcircle.png"
        Background="{StaticResource BackgroundDarkBrush}">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Stretch" Margin="0,0,0,30">
            <Image Source="Resources/mck.png" HorizontalAlignment="Stretch" Stretch="Uniform"/>
            <TextBlock Text="Keeper Configuration Creator" FontSize="22" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}" 
                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
        
        <!-- Info Card -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Environment Variable Details" FontSize="14" FontWeight="SemiBold" 
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Name:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
                    <TextBlock Grid.Column="1" Text="KeeperConfig" Foreground="{StaticResource AccentBrush}" 
                               FontFamily="Consolas" FontSize="13" FontWeight="SemiBold"/>
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Scopes:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                            <TextBlock Text="User" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,4">
                            <TextBlock Text="System" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Status Area -->
        <Border Grid.Row="2" x:Name="StatusBorder" Visibility="Collapsed" 
                Background="{StaticResource StatusSuccessBackgroundBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="StatusIcon" Text="✓" FontSize="16" Foreground="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusTitle" Text="Success" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>
                <TextBlock x:Name="StatusMessage" Text="" Foreground="{StaticResource TextSecondaryBrush}" 
                           FontSize="12" Margin="24,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
        
        <!-- Action Button -->
        <Button Grid.Row="3" x:Name="BtnApply" Style="{StaticResource PrimaryButton}" 
                HorizontalAlignment="Stretch" Padding="20,14" Click="BtnApply_Click">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔧" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="Apply KeeperConfig" FontSize="14" FontWeight="SemiBold"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
